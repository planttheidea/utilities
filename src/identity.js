/**
 * @function identity
 *
 * @description
 * a function always returning the first thing passed
 *
 * @param {any} value the value passed
 * @returns {any} the first parameter passed
 */
export const identity = (value) => value;

export default identity;
